import React from 'react';
import Menu_Button from '../../assets/menu_button.svg';

const MenuButton = () => (
    <img src={Menu_Button} className="menu_button" alt="menu_button" title="menu_button"/>
);

export default MenuButton;